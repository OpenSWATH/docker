name: build-and-publish

on:
  release:
    types: [published]

permissions:
  contents: read
  packages: write

env:
  IMAGE_OWNER: openswath
  IMAGE_NAME: openswath
  REGISTRY: ghcr.io

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compute tags
        id: meta
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          echo "TAG_SHA=sha-${SHORT_SHA}" >> $GITHUB_ENV
          echo "TAG_RELEASE=${GITHUB_REF_NAME}" >> $GITHUB_ENV   # e.g. v0.3.0
          # Set LATEST for non-prerelease releases
          if [ "${{ github.event.release.prerelease }}" = "false" ]; then
            echo "TAG_LATEST=latest" >> $GITHUB_ENV
          fi

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.IMAGE_NAME }}:${{ env.TAG_SHA }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.IMAGE_NAME }}:${{ env.TAG_RELEASE }}
            ${{ env.TAG_LATEST && format('{0}/{1}/{2}:{3}', env.REGISTRY, env.IMAGE_OWNER, env.IMAGE_NAME, env.TAG_LATEST) || '' }}
          build-args: |
            OPENMS_TAG=3.4.1
            MAKE_JOBS=8
            PYPROPHET_VERSION=3.0.2
            EASYPQP_VERSION=0.1.53
            ARYCAL_URL=${{ vars.ARYCAL_URL }}
            SAGE_URL=${{ vars.SAGE_URL }}
